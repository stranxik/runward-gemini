{
  "name": "runward-gate",
  "version": "0.18.1",
  "description": "runward's deterministic zero-LLM gate, surfaced at end of turn (AfterAgent). Advisory in the harness: it prints the verdict of `runward check --strict`; the hard gate stays in CI."
}
